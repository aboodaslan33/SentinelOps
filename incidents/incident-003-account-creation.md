# Incident Report — INC-2026-003

> **LABORATORY DATA.** Generated by the lab owner in an isolated virtual environment using
> `New-LabTestAccount.ps1`. The account described was created and removed as part of a planned
> detection test.

---

## 1. Incident summary

| Field | Value |
|---|---|
| **Incident ID** | INC-2026-003 |
| **Date** | 2026-08-28 |
| **Time (UTC)** | Account created 10:22:14 · Group change 10:22:52 · Alert 10:22:54 · Triage 10:25 |
| **Alert name** | SentinelOps: Newly created account lab-svc-update was added to a privileged group within 5 minutes — possible backdoor account |
| **Wazuh rules** | `100120` (level 8), `100122` (level 5), `100140` (level 12), `100121` (level 13) |
| **Severity** | **High** (Wazuh level 13) — unauthorised persistence with privilege |
| **Affected host** | `WIN-SOC-EP01` (192.168.56.20, agent ID 001) |
| **Account created** | `lab-svc-update` (local, SID recorded in evidence) |
| **Created by** | `WIN-SOC-EP01\lab-admin` (logon ID `0x3E7A1`) |
| **Source IP** | `192.168.56.30` — the creating session was a **network logon (type 3)**, not console |
| **Detection source** | Windows Security event log (4720, 4722, 4724, 4732) + Sysmon EID 1 |
| **Analyst** | SOC L1 (lab owner) |
| **Final status** | **Closed — True Positive (authorised lab simulation), eradicated** |

---

## 2. Description

At 10:22:14 UTC a new local account, `lab-svc-update`, was created on `WIN-SOC-EP01`. The
account was enabled and given a password in the same second, and **38 seconds later** it was
added to the local **Administrators** group.

Wazuh raised three escalating alerts: `100120` (Medium — account created), `100140` (High —
privileged group change) and `100121` (High, level 13 — the *correlation* between creation and
promotion inside the 5-minute window).

The account name was chosen to blend in with legitimate service accounts (`lab-svc-update`
reads like an update service). This is a textbook **backdoor administrator account**: durable
across reboots, invisible to antivirus, and created entirely with signed Windows binaries.

---

## 3. Timeline (UTC)

| Time | Event | Source | Evidence |
|---|---|---|---|
| 10:19:47 | Network logon (type 3) for `lab-admin` from `192.168.56.30` | Security 4624 | Logon ID `0x3E7A1` |
| 10:19:47 | Special privileges assigned to that logon (`SeDebugPrivilege`, `SeBackupPrivilege`, …) | Security 4672 | Rule `100141` |
| 10:22:12 | `cmd.exe` → `net.exe user lab-svc-update ******** /add` | Sysmon EID 1 | PID 6104, parent PID 5992 |
| 10:22:13 | `net.exe` → `net1.exe` (Windows internally delegates the subcommand) | Sysmon EID 1 | Corroboration |
| **10:22:14** | **Local account `lab-svc-update` created** | **Security 4720** | **Rule `100120` (level 8)** |
| 10:22:14 | Account enabled | Security 4722 | Rule `100122` |
| 10:22:14 | Password set on the account | Security 4724 | Rule `100122` |
| 10:22:50 | `cmd.exe` → `net.exe localgroup administrators lab-svc-update /add` | Sysmon EID 1 | PID 6180 |
| **10:22:52** | **`lab-svc-update` added to Administrators** | **Security 4732** | **Rules `100140` (12), `100121` (13)** |
| 10:22:54 | High alert visible on the dashboard | Wazuh manager | — |
| 10:25:00 | L1 triage started | Analyst | — |
| 10:31:00 | Confirmed no logon had yet occurred as the new account | Security 4624 search | Negative result |
| 10:34:00 | Account **disabled** (not deleted — evidence preservation) | Analyst | — |
| 10:36:00 | Removed from Administrators | Analyst | — |
| 10:40:00 | Creating session (`0x3E7A1`) fully reviewed | Analyst | See §5 |
| 10:52:00 | Account removed, evidence exported, incident closed | Analyst | This report |

**MTTD: 40 seconds** (account creation → correlated High alert).
**Time to triage: 3 minutes.** **Time to contain: 12 minutes.**

---

## 4. Indicators of compromise (lab)

| Type | Value |
|---|---|
| Account created | `lab-svc-update` |
| Account SID | Recorded in the evidence export (`localusers.csv`) |
| Creating account | `WIN-SOC-EP01\lab-admin` |
| Creating logon ID | `0x3E7A1` (logon type 3 from `192.168.56.30`) |
| Commands observed | `net user lab-svc-update ******** /add` · `net localgroup administrators lab-svc-update /add` |
| Processes | `cmd.exe` (PID 5992) → `net.exe` (PID 6104, 6180) → `net1.exe` |
| Group modified | `Administrators` (local) |
| Time between creation and promotion | 38 seconds |
| Event IDs | 4720, 4722, 4724, 4732, and 4624/4672 for the creating session |

> Passwords are **never** recorded in Windows event logs, in Wazuh alerts, or in this report.
> The command line is masked here as `********` for the same reason — real 4688/Sysmon command
> lines for `net user` **do** contain the plaintext password, which is itself a finding
> (see recommendation #5).

---

## 5. Investigation

**Q1 — Was this an authorised change?**
No change ticket existed for account creation on this host. In a production environment this
alone justifies escalation.

**Q2 — Who created it, and how did they get there?**
`subjectUserName` = `lab-admin`, `subjectLogonId` = `0x3E7A1`. Pivoting on the logon ID:

```
data.win.eventdata.logonId:"0x3E7A1" AND data.win.system.eventID:4624
```

returned a **logon type 3 (network) from 192.168.56.30** at 10:19:47 — i.e. the account was
created by a **remote** session, not by someone sitting at the machine. That is the single most
important finding in this incident: the actor was already remote-capable before the account
was created.

**Q3 — What exactly ran?**
Sysmon EID 1 shows `net.exe user … /add` and `net.exe localgroup administrators … /add`, with
`cmd.exe` as the parent. Note that Windows delegates to `net1.exe`, which is why the Sysmon
config and rules cover both binaries — a rule watching only `net.exe` would show half the story.

**Q4 — Is the new account privileged?**

```powershell
Get-LocalGroupMember -Group "Administrators"
net user lab-svc-update
```

Confirmed member of **Administrators**. Not a member of Remote Desktop Users, so no RDP path
was created.

**Q5 — Has the account been used?**

```
data.win.eventdata.targetUserName:"lab-svc-update" AND data.win.system.eventID:4624
```

**No results.** The backdoor was created but not yet used — the detection caught it inside the
same minute.

**Q6 — Is there other persistence?**
Checked 4698 (tasks), 7045 (services), registry Run keys (Sysmon EID 12/13), Startup folder
(EID 11) for the 24 hours around the event. **None found.** The account was the only
persistence mechanism.

**Q7 — Were other accounts touched?**
No other 4720/4726/4732 events in the period. `Get-LocalUser` output was compared against the
baseline captured at the `03-fully-instrumented` snapshot; `lab-svc-update` was the only
difference.

---

## 6. MITRE ATT&CK mapping

| Tactic | Technique | ID | Evidence |
|---|---|---|---|
| Persistence (TA0003) | Create Account: Local Account | [T1136.001](https://attack.mitre.org/techniques/T1136/001/) | Event 4720 — `lab-svc-update` created |
| Persistence (TA0003) / Privilege Escalation (TA0004) | Account Manipulation: Additional Local or Domain Groups | [T1098.007](https://attack.mitre.org/techniques/T1098/007/) | Event 4732 — added to local Administrators |
| Persistence (TA0003) | Account Manipulation | [T1098](https://attack.mitre.org/techniques/T1098/) | Events 4722 / 4724 — enabled and password set |
| Defense Evasion / Persistence / Privilege Escalation | Valid Accounts: Local Accounts | [T1078.003](https://attack.mitre.org/techniques/T1078/003/) | Creating session was a type-3 logon by an existing privileged account |

---

## 7. Findings

1. **An unauthorised privileged local account was created and promoted within 38 seconds.**
2. **The creating session was remote (logon type 3 from 192.168.56.30)**, which means the actor
   already had valid administrative credentials for this host before the incident began.
   *The account creation is a symptom; the credential compromise is the root cause.*
3. **The account was never used to authenticate** — detection and containment preceded use.
4. **The naming was deliberately camouflaged** (`lab-svc-update` mimics a legitimate service
   account), which is exactly why the detection is built on the *action* rather than on
   suspicious-looking names.
5. **Correlation added real value.** `100120` alone is Medium; correlating creation with
   promotion produced a level-13 alert that could not reasonably be ignored.
6. **No other persistence existed**, so eradication was straightforward.

---

## 8. Response actions taken

| Time (UTC) | Action | Result |
|---|---|---|
| 10:34 | `Disable-LocalUser -Name "lab-svc-update"` — **disabled, not deleted** | Complete |
| 10:36 | `Remove-LocalGroupMember -Group "Administrators" -Member "lab-svc-update"` | Complete |
| 10:38 | Evidence exported (Security + Sysmon `.evtx`, `localusers.csv`, hashes) | Complete |
| 10:40 | Reviewed everything the creating session (`0x3E7A1`) did | No further actions found |
| 10:44 | `lab-admin` password rotated (the credential that enabled the creation) | Complete |
| 10:46 | Host firewall block for `192.168.56.30` | Applied |
| 10:50 | Account removed after evidence capture | Complete |
| 10:52 | Verified `Get-LocalUser` / `Get-LocalGroupMember` match the baseline | Confirmed |

**Why disable before delete:** deleting the account destroys the SID, and the SID is what links
this account to any later event in the timeline. Disable → investigate → preserve → then remove.

In a production incident this would have been **escalated to L2/IR** at triage: unauthorised
administrative access plus a remote creating session meets the escalation criteria in
`/documentation/incident-response.md`.

---

## 9. Recommendations

| # | Recommendation | Priority | Rationale |
|---|---|---|---|
| 1 | Treat every 4720 on a workstation as an incident until proven otherwise | High | The base rate is near zero; the cost of review is minutes |
| 2 | Investigate how `lab-admin` credentials were usable remotely — this is the real root cause | High | The account creation was the second step, not the first |
| 3 | Deploy Windows **LAPS** so local admin passwords are unique per host | High | Stops one compromised local admin password from working everywhere |
| 4 | Restrict network logon (type 3) for local administrator accounts (*Deny access to this computer from the network*) | High | Removes the exact path used here |
| 5 | Prefer `New-LocalUser` over `net user` in admin runbooks | Medium | `net user <name> <password>` puts the **plaintext password in the command line**, which is then logged by Sysmon/4688 and shipped to the SIEM |
| 6 | Add a weekly scheduled review of local accounts and Administrators membership | Medium | Catches accounts created while logging was down |
| 7 | Alert on any local account whose name matches `svc-*` being added to Administrators | Low | Service accounts should not be interactive administrators |

---

## 10. Lessons learned

* **Correlation beats single events.** The creation alert alone would likely have been triaged
  as routine IT activity; the 38-second link to a privileged group made the intent undeniable.
* **Always pivot on `subjectLogonId`.** It converted "an admin created an account" into "a
  *remote* session created an account", which changed the entire incident.
* **`net user` leaks passwords into telemetry.** A genuine operational finding from the lab,
  now reflected in the hardening recommendations.
* **Disable, do not delete.** Evidence preservation discipline was applied and documented.
* **Detection gap identified:** no alert exists for *account creation outside business hours*.
  Raised as SO-DET-003-B.

**Final status: CLOSED — True Positive (authorised laboratory simulation), eradicated, host
returned to baseline.**
